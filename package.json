{
  "name": "mpex",
  "version": "1.0.0",
  "description": "Money Puzzle Exchanger HTML5 remake",
  "main": "src/main.ts",
  "scripts": {
    "build": "./node_modules/.bin/esbuild src/main.ts --bundle --loader:.png=dataurl --outfile=dist/out.js",
    "start": "http-server dist",
    "serve": "./node_modules/.bin/esbuild src/main.ts --bundle --loader:.png=dataurl --outfile=dist/out.js && http-server dist"
  },
  "author": "mgurga",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.23.0",
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
